# テスト計画 — {{display_name}}

> status: 03-test-plan.status を参照
> 前提: 02-design.status が `approved`

## 1. テスト方針（テストピラミッド）

正本: `.cursor/rules/testing-pyramid.mdc`

### 1.1 レイヤ分担

| レイヤ | ツール | 役割 | 本機能での対象 |
|--------|--------|------|----------------|
| 上（少） | Playwright E2E | 重要ジャーニー **正常系** ＋ **ビジネスクリティカル異常系** | |
| 中（多） | PHPUnit | **単体**（ロジック・境界値・異常系）＋ **結合**（DB・Service 連携） | |
| 中（多） | Vitest | JS **単体**（純関数・状態管理） | |

| 種別 | 対象 | ツール | 備考 |
|------|------|--------|------|
| E2E | UC 主要フロー、権限、ジャーニー上の UI | Playwright | `inp` はスモークのみ。境界値網羅は載せない |
| 単体（サーバー） | Service / FormRequest / Enum / Model | PHPUnit | `layer:unit`（FormRequest 含む） |
| HTTP 検証 | POST 等 → 422 / エラー | PHPUnit | `layer:http`（Controller 単体は作らない） |
| 結合（サーバー） | Service + DB | PHPUnit | CSV 備考に `layer:integration` |
| 単体（フロント） | JS 純関数 | Vitest | DOM 非依存を優先 |

### 1.2 E2E に載せないもの（PHPUnit / Vitest へ）

- 境界値の NG/OK ペア（文字数、行数、件数等）
- 複合 VAL のエラー理由ごとの網羅
- Service 内分岐の総なめ

### 1.3 複合 VAL 分解表（必須）

`01-requirements.md` §7 で 1 行に複数ルールが括られている VAL について、
**サブルールごと**に Test ID を割り当てる。PHPUnit に 1 ケース以上を原則とする。

列定義は `.cursor/rules/testing-pyramid.mdc`「成果物フォーマット」に従い、**以下の列を変更しない**。

| 要件 ID | サブルール・観点 | 期待値 | テストレイヤ | Test ID / E2E-only の理由 |
|---------|------------------|--------|--------------|---------------------------|
| VAL-18 | （例）メール形式不正 | 422 または errors 1 件 | PHPUnit | PU-0xx |
| VAL-18 | （例）ファイル内メール重複 | 422 または errors 1 件 | PHPUnit | PU-0xx |

### 1.4 閾値チェック一覧（必須）

| 項目名 | 境界値 / 条件 | OK ケース（レイヤ / Test ID） | NG ケース（レイヤ / Test ID） | E2E スモーク（画面 / Test ID） |
|--------|---------------|-------------------------------|-------------------------------|--------------------------------|
| （例）パスワード | 8 文字未満 / 8 文字以上 | PHPUnit: PU-0xx | PHPUnit: PU-0xx | ユーザー登録 / E2E-0xx または `-` |

- **E2E スモーク**は **1 画面（フォーム）1 ケース**。入力項目数ぶん行や E2E を増やさない
- NG/OK ペアは **PHPUnit または Vitest** に置く

### 1.5 E2E 棚卸し（必須）

正本: `.cursor/rules/testing-pyramid.mdc`「フェーズ 3 の作成手順」「E2E 棚卸しの判定」

E2E CSV を確定する前に、候補ごとに **維持 / 移行 / 削減** を判定する。
**移行** は PHPUnit / Vitest の Test ID を先に書いてから記載する。

| Test ID（候補） | カテゴリ | 観点 | 判定 | 理由 | 移行先 Test ID（移行時） |
|-----------------|----------|------|------|------|--------------------------|
| E2E-0xx | evt | （例）管理者のユーザー登録 | 維持 | UC-07 代表ジャーニー | — |
| E2E-0yy | auth | （例）他社 URL 直打ち 403 | 移行 | HTTP レスポンスのみで十分 | PU-0zz |
| E2E-0zz | evt | （例）SU と管理者の同一登録 | 削減 | ロール違いのみ。代表は E2E-0xx | — |

- **維持** と判定した行だけを `03-test-plan.csv` に残す
- 判定 **移行** の観点は E2E CSV に残さない（下層 CSV にあることを確認）
- 件数比は参考として §2 に記載する。高くても、上記定性チェックと §1.5 の理由が揃っていればよい

### 1.6 移行サマリ（再設計・旧計画からの改定時のみ）

旧計画からの再配分を行った場合のみ記載する。

| 種別 | 旧件数 | 新件数 | 差分 | 主な変更 |
|------|--------|--------|------|----------|
| E2E | | | | |
| PHPUnit | | | | |
| Vitest | | | | |

## 2. テスト種別別・カテゴリ別件数

CSV はテスト種別ごとに分けて作成する。E2E / PHPUnit / Vitest のケースを同じ CSV に混在させない。

| 種別 | CSV | 件数 | 主な確認内容 |
|------|-----|------|--------------|
| E2E | `03-test-plan.csv` | | ジャーニー正常系、権限、UI スモーク |
| PHPUnit | `03-test-plan-phpunit.csv` | | ロジック・境界値・異常系・DB 結合 |
| Vitest | `03-test-plan-vitest.csv` | | JS 単体 |

**件数比（参考）**: E2E / 全テストの比率は、下層へ正しく寄せたとき **1 割前後** になりやすい経験則がある
（`testing-pyramid.mdc`）。**上限ではない**。§1.5 の **維持** 件数と本表の E2E 件数が一致すること。

| カテゴリ | 接尾辞 | E2E | PHPUnit | Vitest | 概要 |
|----------|--------|-----|---------|--------|------|
| 入力項目定義 | inp | | | | **PHPUnit 主**。E2E はスモークのみ |
| 権限 | auth | | | | HTTP 認可は PHPUnit（`layer:http`）。E2E は画面挙動の代表のみ |
| イベント | evt | | | | ジャーニー正常系（E2E）＋ ロジック・絞り込み（PHPUnit） |
| 表示確認 | dsp | | | | UI 描画（E2E）＋ 表示データの計算（PHPUnit / Vitest） |
| 画面遷移 | trn | | | | ジャーニー正常系（E2E） |
| 動的挙動 | dyn | | | | JS ロジック（Vitest 主） |
| その他 | other | | | | 性能・ハッシュ等（PHPUnit / Vitest） |

## 3. 網羅基準

- [ ] テストピラミッド（§1）に従い、境界値・VAL 網羅が PHPUnit / Vitest 側にあること
- [ ] 複合 VAL 分解表（§1.3）が埋まっていること
- [ ] 閾値チェック一覧（§1.4）で NG/OK ペアが PHPUnit または Vitest にあること
- [ ] E2E の `inp` が **1 画面 1 スモーク**＋クリティカル異常に収まっていること（項目数ぶんの inp を書いていないこと）
- [ ] FormRequest / HTTP（422）の VAL が PHPUnit に計画されていること
- [ ] §1.5（E2E 棚卸し）が完成し、E2E CSV は **維持** 判定のみであること
- [ ] 異常系（バリデーション、存在しない ID 等）— **PHPUnit / Vitest で網羅**（E2E はスモーク代表のみ）
- [ ] 境界値（最大文字数、0件、上限件数等）— **PHPUnit / Vitest で NG/OK ペア**
- [ ] 権限パターン（ロール別の操作可否）— **PHPUnit HTTP で網羅**、E2E は画面挙動の代表のみ
- [ ] 派生パターン（組み合わせ・状態遷移）— **ロジックは PHPUnit**、通しジャーニーは E2E 代表
- [ ] 更新系の空入力（下記「更新系の標準観点」を項目ごとに確認）
- [ ] CSV の各行が 1 ケース 1 観点になっていること（対象項目・条件・期待結果が異なる場合は分割）
- [ ] 業務ルール・入力検証・権限・状態判定について、正常に通る代表ケースと異常系を対で確認していること
- [ ] 要件で許可されている操作・状態遷移をすべて列挙し、操作ごとに個別ケースを作成していること（一部の操作だけ確認して残りを省略しない）
- [ ] 条件分岐の取り得る値（例: 制限あり・一致 / 制限あり・不一致 / 制限なし）ごとにケースを作成していること
- [ ] 要件カバレッジ表（下記「要件カバレッジ」）を作成し、対応漏れがないこと（非機能要件 NFR-xx を含む）
- [ ] 期待結果の書き方（下記「期待結果の書き方」）に沿い、曖昧な表現を使っていないこと
- [ ] 独立性・再現性（下記「独立性・再現性の標準観点」）を満たしていること
- [ ] `02-design.md` §5 で「影響あり」とした項目に、回帰確認のテストケースが対応していること（下記「回帰確認」）

### 要件カバレッジ（トレーサビリティ）

`01-requirements.md` の下記項目それぞれについて、対応する Test ID が **最低 1 件** 存在することを
このカバレッジ表で確認する。CSV の「出典」列には、対応する要件側 ID（`UC-xx` / `VAL-xx` / `AC-xx` /
`NFR-xx` / 画面項目名 / `§8` のロール×操作、回帰確認の場合は `IMPACT-xx`）を記載する。
対応漏れ（Test ID 列が空）がないことを確認してから承認依頼する。

| 要件 ID | 種別 | 概要 | 対応 Test ID | 備考 |
|---------|------|------|--------------|------|
| UC-01 | ユースケース（§4） | | | |
| VAL-01 | バリデーション（§7） | | | |
| AC-01 | 受け入れ条件（§11） | | | |
| NFR-01 | 非機能要件（§9） | | | |
| （画面項目名） | 画面項目（§5.2） | | | |
| （ロール×操作） | 権限（§8） | | | |

<!-- 行は要件の実項目数に合わせて増減する。対象外と判断した項目は「備考」に理由を明記する -->

非機能要件（NFR-xx）は E2E での検証が困難なものが多い。その場合は「対応 Test ID」を
PHPUnit のケースにするか、備考に「対応不可・理由・代替手段（手動確認等）」を明記する。

### 回帰確認（02-design.md §5 影響範囲との対応）

`02-design.md` §5 で「影響あり」とした項目について、対応するテストケースをここで確認する。

| 影響 ID | 対象 | 対応 Test ID | 備考 |
|---------|------|--------------|------|
| IMPACT-01 | | | |

<!-- 「影響なし」と判断した項目はここに載せなくてよい（02-design.md 側に理由が書かれていればよい） -->

### 期待結果の書き方（曖昧表現の排除）

期待結果は、実行者や時期によって判定がぶれないよう、**検証可能な具体的な値**で書く。
「正しく」「適切に」「問題なく」等、判定基準を含まない表現は使わない。

| NG 例 | OK 例 |
|--------|--------|
| 正しく表示される | 一覧に該当データが 5 件、氏名の昇順で表示される |
| 適切にエラーになる | 氏名欄の下に「氏名を入力してください」が表示され、保存されない（DB 件数が変化しない） |
| 問題なく保存される | 一覧画面に遷移し、登録した氏名・メールアドレスが 1 行目に表示される |

- バリデーションエラーは「エラーになること」だけでなく、**エラーメッセージの文言**まで固定する
- 画面表示は「表示される」だけでなく、**件数・並び順・表示位置**等、確認可能な粒度まで書く

### 独立性・再現性の標準観点

各テストケースは、他のケースの実行結果や実行順序に依存せず、**単独で実行しても成立する**ように書く。

- [ ] 前提条件は、そのケース単独で再現可能なデータ状態を明記している（他の Test ID が作成したデータを暗黙の前提にしない）
- [ ] 複数のケースが同一の共有データ（マスタ・既存レコード等）を書き換える場合、ケース間で干渉しないようテストデータを分離している（例: ケースごとに固有のレコードを新規作成してから使う）
- [ ] 実行順序を入れ替えても、または他のケースを実行せずに単体で実行しても、期待結果が成立する
- [ ] Seeder 等の共通初期データに依存する場合は、その旨を前提条件に明記している（「§4 テストデータ」の内容と矛盾しない）

#### 並列実行安全性（E2E は並列実行が前提）

E2E は複数ワーカーで同時に実行される前提で書く。「単独実行なら成立する」だけでは不十分で、
**他のケースと同時に実行されても干渉しない**ことを確認する。

- [ ] 一覧の 1 行目・件数等、Seeder の初期データをそのまま参照するケースは、他のケースがその
      データを更新・削除しないことを確認している（更新系のケースは別レコードを使う）
- [ ] 同一レコード（同一ユーザー、同一マスタ行等）を複数のケースが更新する場合、ケースごとに
      専用のレコードを新規作成してから操作する（既存レコードの奪い合いにしない）

### CSV ケース粒度の標準観点

各 CSV は **1 行 1 観点** を原則とする。

| 分割が必要な例 | 分割方針 |
|----------------|----------|
| 複数の入力項目をまとめている | 入力項目ごとに別ケースにする |
| 複数のエラー理由をまとめている | エラー理由ごとに別ケースにする（例: 未指定、形式不正、存在なし） |
| 複数のロールや権限をまとめている | ロールや権限パターンごとに別ケースにする |
| 複数の状態・ステータスをまとめている | 期待結果や遷移が異なる場合は状態・ステータスごとに別ケースにする |

複数条件を 1 行にまとめるのは、同じ操作・同じ期待結果として同一の確認観点である場合に限る。

### 更新系の標準観点（編集機能がある場合は必須）

要件の画面項目定義（必須/任意）に照らして、**全項目**について以下を確認する。

| 項目種別 | 空にして更新した場合の期待動作 | テストケース |
|----------|-------------------------------|-------------|
| 必須項目 | バリデーションエラーとなり、DB は元の値のまま | 必ず作成 |
| 任意項目（空入力 OK） | **空値（null / 空文字）で上書き保存される**（「空なら元の値を維持」にしない） | 必ず作成 |

※「空なら元の値を維持する」仕様にしたい場合は、要件定義（01-requirements.md）に明記されていることを確認する。明記がなければ open-questions で確認する。

## 4. テストデータ

<!-- Seeder / 前提データ / テストユーザー -->

## 5. E2E ファイル対応（実装時）

| spec ファイル | CSV |
|--------------|-----|
| | 03-test-plan.csv |

## 6. Vitest 対応（該当時）

| テストファイル | 対象 JS | 概要 |
|--------------|---------|------|
| | | |

## 7. PHPUnit 対応（該当時）

| テストファイル | 対象 | layer | 概要 |
|--------------|------|-------|------|
| | | unit / integration | |

備考列（CSV）に `layer:unit` または `layer:integration` を記載する。

## 8. 詳細ケース

詳細はテスト種別ごとの CSV を参照。

| 種別 | CSV |
|------|-----|
| E2E | `03-test-plan.csv` |
| PHPUnit | `03-test-plan-phpunit.csv` |
| Vitest | `03-test-plan-vitest.csv` |
